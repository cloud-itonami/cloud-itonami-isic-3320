{:paths ["src"]
 ;; langgraph-clj brings langchain-clj transitively (both zero-/single-dep,
 ;; all .cljc -> portable to JVM / SCI / ClojureScript / GraalVM). It lives
 ;; in the kotoba-lang org of this workspace. This repo is standalone
 ;; (forkable outside the workspace); the :local/root paths only resolve
 ;; inside the monorepo checkout -- a standalone fork should override them
 ;; with git coordinates. `installation.store` uses `langchain-store.core`
 ;; for the shared EDN-blob codec / identity-schema / entity field-spec
 ;; machinery (ADR-2607141600) instead of hand-rolling it. This actor is
 ;; COORDINATION-ONLY (every proposal effect is `:propose`) -- it
 ;; deliberately has NO dependency on `kotoba-lang/robotics` or
 ;; `kotoba-lang/physics-2d`: it never simulates or dispatches heavy-lift/
 ;; rigging-equipment actions, only proposes, schedules, flags and logs,
 ;; all governor- and human-gated (see README). This build also
 ;; deliberately ships NO JVM-only interop anywhere in `src/` (no
 ;; `java.net.http`, no real Resend/Twilio transports in
 ;; `installation.notify` -- only the portable mock notifier) per this
 ;; workspace's cljs-first `.cljc`-runtime-priority rule (CLAUDE.md
 ;; `.cljc`/`.kotoba` runtime priority section) -- a real transport can be
 ;; added later via a portable (cljs/nbb) HTTP client without touching this
 ;; namespace's protocol shape.
 ;; jp-go-digital-design-system (DADS, デジタル庁デザインシステム) is this
 ;; workspace's BASE design system (CLAUDE.md "UI/UX 標準"). It is consumed
 ;; ONLY by `installation.render-html`, the build-time console generator --
 ;; a `.clj` tool, not part of the portable actor surface under
 ;; `src/installation/*.cljc`, so the no-JVM-interop mandate above is
 ;; unaffected. Pinned by :git/sha, not :local/root, so a standalone fork
 ;; resolves it too.
 :deps {
        ;; kotoba.lang.text, not clojure.string. Pinned as an explicit floor:
        ;; tools.deps takes the NEWEST sha it is shown, so without a floor here
        ;; this repo silently rides whatever a sibling happens to name.
        io.github.kotoba-lang/text {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}
       io.github.kotoba-lang/jp-go-digital-design-system
        {:git/url "https://github.com/kotoba-lang/jp-go-digital-design-system"
         :git/sha "83db99546eabc53613ae826ea41d1533a3c574af"}
        io.github.com-junkawasaki/langgraph-clj
        {:local/root "../../kotoba-lang/langgraph"}
        io.github.kotoba-lang/langchain-store
        {:local/root "../../kotoba-lang/langchain-store"}}
 :aliases
 {;; This alias held an :override-deps entry for the retired coordinate
  ;; io.github.com-junkawasaki/langchain-clj. langgraph declares the same
  ;; library as io.github.kotoba-lang/langchain, so the entry matched no
  ;; node in the resolved tree and never reached the classpath -- verified
  ;; with `kbb -Stree`/-Spath, unchanged before and after removal.
  :dev {}
  ;; kbb -M:dev:run  -> drive the demo through one OperationActor
  :run {:main-opts ["-m" "installation.sim"]}
  ;; kbb -M:dev:render-html [out-file] -> regenerate
  ;; docs/samples/operator-console.html by driving the REAL actor stack
  ;; (flagship checklist item 2). Deterministic: two runs are byte-identical.
  :render-html {:main-opts ["-m" "installation.render-html"]}
  :test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd6605cb6c0efc275e1df1736f6e90d4d73"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  ;; static analysis -- clj-kondo from deps (no binary), errors fail CI.
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts    ["-m" "clj-kondo.main"
                        "--lint" "src" "test"
                        "--fail-level" "error"]}}}
